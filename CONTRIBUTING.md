# Contributing

Contributions are welcome.

1. Open an issue for behavior changes.
2. Install dependencies with `npm ci`.
3. Add or update tests.
4. Run `npm run check` and `npm pack --dry-run`.
5. Submit a focused pull request.

Please keep the runtime dependency-free and the public API DOM-free.
